# Pi-hole Deployment

## Purpose
Pi-hole was deployed as an initial containerised service to validate container networking and DNS functionality.

## Deployment
- Docker Compose used for service management
- Web interface exposed via port mapping
- Persistent volumes configured

## Outcome
The Pi-hole dashboard was accessible from the host system and confirmed successful container deployment.
This validates container networking within the lab.
